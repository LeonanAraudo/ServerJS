class User { 
    constructor(username, password) {  
    this.username = username;  
    this.password = password;  
    }  
   }  
   module.exports = User;  
   
//Primeiro é criado a classe User
//Logo em seguida e criado o metodo constructor que é usado para iniciar os objetos a partir da classe
//Quando um usuário é criado os parametros username e password são passados como argumento
//Dentro do construtor são criadas duas propriedades que vão armazenar os vallores de username e password
